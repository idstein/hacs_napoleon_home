pytest==8.3.*
pytest-asyncio==0.24.*
pytest-cov==5.*
pytest-homeassistant-custom-component==0.13.*
ruff==0.7.*
mypy==1.13.*
aioresponses==0.7.*
# Transitive pin: acme 2.11 (via homeassistant -> hass-nabucasa) still uses
# josepy.ComparableX509 which was removed in josepy 2.x. Without this pin,
# pytest plugin autoload crashes on import.
josepy<2
